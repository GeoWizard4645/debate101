# Local config containing API keys — never commit
config.js

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json

# Main site build (Vite + React). package-lock.json IS committed — the deploy
# workflow runs `npm ci`, which requires it.
node_modules/
dist/

# Cascade desktop (Electron) build artifacts
desktop/node_modules/
desktop/dist/
desktop/out/

# Claude Code worktrees
.claude/worktrees/
